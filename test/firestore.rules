
service cloud.firestore {
  match /databases/{database}/documents {
    match /__reflect {
      match /deny { allow read: if false; allow write: if false; }
      match /allow { allow read: if true; allow write: if true; }
    }
  }
}